<UserControl x:Class="FullScratchCore.Views.CustomTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:FullScratchCore.ViewModels"
             xmlns:local="clr-namespace:FullScratchCore.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:CustomTextBoxViewModel/>
    </UserControl.DataContext>
    <Grid>
        <!--<TextBox Text="{Binding SelectedItemText, Mode=OneWay}"/>-->
        <TextBox Text="{Binding FullPath , Mode=TwoWay}" FontSize="20"/>
    </Grid>
</UserControl>
